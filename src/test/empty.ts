// Stub used by vitest to replace the `server-only` guard, which intentionally
// throws when imported outside a React Server Component graph. Tests exercise
// the server reader functions directly in Node, so the guard is a no-op here.
export {};
